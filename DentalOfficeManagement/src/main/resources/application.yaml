spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dental_db
    username: root
    password: tu_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

jwt:
  # Clave secreta en Base64 (256 bits). Cambiala en producción.
  secret: J4wwDwl8p80eX0OxeLFa2gt2M2WV1HAfBevophvKq6s=
  # Duración del token en milisegundos (acá: 24 horas).
  expiration: 86400000